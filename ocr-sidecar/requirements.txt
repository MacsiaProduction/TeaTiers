# OCR sidecar runtime pins (research 13 / decision #105). Scanned by the OSV-Scanner CI gate.
# rapidocr + onnxruntime versions are the run-13-locked pair; the rest is the FastAPI surface.
rapidocr==3.8.4
onnxruntime==1.27.0
fastapi==0.137.2
uvicorn[standard]==0.49.0
python-multipart==0.0.32
pillow==12.2.0
# headless (no GUI/X11 libs); rapidocr pulls full opencv-python, so the Dockerfile swaps to this.
opencv-python-headless==4.13.0.92
numpy==2.4.6
