# OCR sidecar runtime pins (research 13 / decision #105). Scanned by the OSV-Scanner CI gate.
# rapidocr + onnxruntime versions are the run-13-locked pair; the rest is the FastAPI surface.
rapidocr==3.8.4
onnxruntime==1.27.0
fastapi==0.138.0
uvicorn[standard]==0.49.0
python-multipart==0.0.32
pillow==12.2.0
# headless (no GUI/X11 libs); rapidocr pulls full opencv-python, so the Dockerfile swaps to this.
opencv-python-headless==4.13.0.92
numpy==2.5.0
# Track-1 OCR name recovery (research run 18 winner opus / decision #123): fuzzy-matches the
# confusable-normalized OCR text against the catalog. MIT, C++/pybind, no extra system libs.
rapidfuzz==3.14.5
# Description-safe OCR correction (research run 19 winner opus / decision #125): pymorphy3 is the
# real-Russian-word oracle that gates homoglyph fixes (Букет, not Вукет). Both MIT.
pymorphy3==2.0.6
pymorphy3-dicts-ru==2.4.417150.4580142
