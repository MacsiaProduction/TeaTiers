# OSV-Scanner ignore list (decision #102). Entries here got a real look, not a blanket suppress —
# see each entry's reason. Revisit whenever the pinned dependency version changes.

[[IgnoredVulns]]
id = "GHSA-5jmj-h7xm-6q6v"
reason = """
jackson-databind case-insensitive @JsonIgnoreProperties bypass. Exploitable only when a type opts
into @JsonFormat(ACCEPT_CASE_INSENSITIVE_PROPERTIES) — grepped, zero usages anywhere in this repo,
so this is not reachable in TeaTiers regardless of version. No fix is cleanly available yet either:
the advisory names 2.18.9/2.21.5/2.22.1/3.1.4 as fixed, but Maven Central has no 2.22.1 yet, and
OSV-Scanner still flags 2.21.5 (verified locally, 2026-07-07 — likely the scanner's matching data
lagging the advisory's prose). Pinned to 2.21.5 in server/build.gradle.kts regardless, since it's
the closest available patch in Boot's own 2.21.x line. Revisit: drop this ignore once either
2.22.1 ships or OSV-Scanner stops flagging 2.21.5.
"""
