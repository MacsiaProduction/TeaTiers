# Add project-specific ProGuard/R8 rules here.
# Release builds are not minified yet (set in build.gradle.kts); when minification is
# enabled for release, add keep rules for kotlinx.serialization models, Retrofit, etc.
