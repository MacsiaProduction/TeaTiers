<?xml version="1.0" encoding="utf-8"?>
<!-- FileProvider roots for sharing exported backups (#26). Only the cache "backups/" dir is
     exposed; nothing else in app storage is reachable through the provider. -->
<paths>
    <cache-path name="backups" path="backups/" />
</paths>
